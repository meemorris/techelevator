package com.techelevator.farm;

public interface Singable {

    String getName(); //these are the two methods that a singable thing has to have, if they had parameters you'd have specify them in the parenthesis as well
    String makeSound();
    String getArticle();


}
